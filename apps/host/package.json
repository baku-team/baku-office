{
  "name": "@baku-office/host",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "typecheck": "astro check",
    "test": "node --import ./test/_cf-register.mjs --experimental-strip-types --disable-warning=ExperimentalWarning --test test/*.test.ts"
  },
  "dependencies": {
    "@baku-office/shared": "*",
    "astro": "^6"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^13",
    "@astrojs/check": "^0.9.4",
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.9.3",
    "wrangler": "^4"
  }
}
