{
  "name": "baku-office",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:host": "npm -w apps/host run build",
    "build:client": "npm -w apps/client run build",
    "typecheck": "npm -w packages/shared run typecheck && npm -w apps/host run typecheck && npm -w apps/client run typecheck"
  }
}
